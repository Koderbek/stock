{% extends 'base.html.twig' %}

{% block body %}
    <h1>Создание грузчика</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('porter_index') }}">Назад к списку</a>
        </li>
    </ul>
{% endblock %}
